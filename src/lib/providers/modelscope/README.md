# ModelScope Provider

ModelScope-specific protocol adapters live here.

Provider-specific fields such as `enable_thinking`, `chat_template_kwargs`,
`X-ModelScope-Async-Mode`, and `X-ModelScope-Task-Type` should stay in this boundary.
